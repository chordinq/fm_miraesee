from __future__ import annotations

from PySide6.QtGui import QColor, QPixmap

# TODO: tint sprites / UI panels (combine with nine_slice for themed widgets).


def color_multiply(source: QPixmap, color: QColor) -> QPixmap:
	raise NotImplementedError("color_multiply — planned for button/popup theming")
